{
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/**/*.{js,ts}": ["eslint --fix"],
    "frontend/src/**/*.{ts,tsx}": ["eslint --fix", "prettier --write"]
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
